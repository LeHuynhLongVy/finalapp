<%= link_to 'All photos', user_photos_path(user_id: current_user.id) %>
<h1><%= @photo.title %></h1>

<%= image_tag(@photo.image.url, alt: 'Image') if @photo.image? %>

<p><%= @photo.description %></p>

<p><%= link_to 'Edit', edit_user_photo_path %></p>
